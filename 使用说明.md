# TrayPilot 0.5.4 — Windows 11 托盘图标管理

双击 `app/TrayPilot.exe` 即可使用。独立的 Windows x64 程序，无需另外安装 .NET，不需要 Windhawk。分发或移动程序时，请一起保留 `app/languages` 文件夹。

首次右键点击本程序托盘图标即可打开菜单；首次左键点击可打开主界面。

## 使用

1. 启动后自动读取当前用户的托盘记录，并与运行中的程序、实际存在的图标核对。
2. 每行显示对应的软件托盘图标。左键双击图标或所在行：显示状态会切换为完全隐藏，折叠菜单内也不显示；软件继续运行。
3. 再次双击隐藏项即可恢复，保留该软件的命中规则。手动显示命中项后，本次运行期间会暂时跳过其自动隐藏，直到再次执行“隐藏规则命中”；重启后按保存的规则运行。隐藏项的图标以 35% 不透明度显示，文字淡化；恢复后按 Windows 原有设置回到任务栏或折叠菜单。
4. 鼠标悬停在图标或所在行可查看软件名称、显示状态、自动隐藏规则、Windows 缓存的托盘提示、进程名、PID、完整路径和图标标识。支持搜索，以及 Ctrl / Shift 点击多选（多选时不切换状态），然后使用 **隐藏选中** / **恢复选中** 批量操作。规则按 EXE 完整路径生效，忽略路径大小写及分隔符差异，同一路径的多个实例、多个图标一起控制。规则通过右键“添加到命中规则”显式添加，手动显示/隐藏不再增删规则。
5. 顶栏菜单中的 **隐藏规则** 显示程序图标、名称与完整路径，包含当前未运行的软件；文件不存在时显示默认图标。删除规则前先恢复图标，恢复失败时保留规则以便重试。可以多选删除；**全部恢复** 清除规则并恢复本工具隐藏的图标。
6. 默认关闭窗口后继续在系统托盘运行，隐藏规则继续生效。通过菜单中的 **退出** 真正退出并恢复隐藏图标；在 **设置** 中可关闭“关闭窗口后保留在托盘运行”，此时关闭窗口也会退出。

程序默认开启 **自动刷新（每 2.5 秒）**，可取消勾选暂停周期检查；已开始的一次检查会完成。关闭后仍可点击 **刷新**，双击或批量操作完成后也会立即更新显示。暂停期间不会自动发现新图标或重新应用隐藏规则，重新开启后继续定期检查。每次启动默认开启。

点击 **列表 / 网格** 切换布局：列表展示状态、进程和路径；网格采用 40 像素图标和更紧凑的间距，每个网格项带边框，隐藏项仍淡化显示。列表与网格仅对鼠标所在项使用强蓝底高亮；选中项保留较淡的选择提示，移开鼠标后不会残留强蓝色。两种布局均支持搜索、选择、Ctrl / Shift 多选、悬浮详情和左键双击切换显示状态。切换布局保留搜索条件与选中项。网格悬浮切换仅重绘前后两个项目，并通过缓冲绘制减少闪烁。设置中的下拉框在浅色、暗色主题下均显示外边框，获得焦点时边框使用强调色。

单击只选择软件。右键图标或所在行打开 TrayPilot 的管理菜单：

- **属性**：通过不可编辑的属性表格查看名称、进程、PID、运行状态、图标状态、隐藏规则、完整路径、托盘提示、启动时间及文件版本等信息，可选中行后点击“复制信息”；未选择行时复制全部信息。
- **显示图标 / 隐藏图标**：按当前实际状态显示菜单文字，点击后切换；同一路径的软件图标一起控制。
- **添加到命中规则**：保存对应 EXE 路径；重复项不可重复添加。规则启用时会应用隐藏，暂停时只保存。列表用“✓ 命中”、网格用角标标记规则项，与当前显示状态无关。
- **结束任务**：直接结束点中图标所属的进程，不结束其他同名进程或子进程。结束前核对启动时间和路径；结束后移除该进程的图标及恢复记录，保留隐藏规则。此操作是强制结束，未保存的数据不会自动保存。TrayPilot 自身请通过 **退出** 菜单结束。

刷新只更新程序显示区域和底部状态信息，搜索框保持可输入，保留已有内容、焦点及光标/选区；刷新期间输入的搜索条件会应用于更新后的结果。

## 开机启动

设置及托盘右键菜单均提供 **开机启动**，默认关闭，两处同步显示当前注册状态。设置中点击保存后生效，取消不修改；托盘菜单点击立即生效，以勾选表示开启。启用后在当前用户登录 Windows 时启动，无需管理员权限；默认进入托盘后台，若关闭了本程序托盘图标则显示主界面。普通手动启动仍显示主界面。关闭此选项会移除本程序的启动项。移动程序后，请从新位置重新启用。

## 托盘菜单、关于与快捷键

左键单击或双击 TrayPilot 托盘图标打开主界面；右键打开快捷管理菜单。快捷管理每页最多显示 10 个程序，通过上一页 / 下一页或鼠标滚轮切换：向下滚动到下一页，向上滚动到上一页，到达首尾页后停止翻页。菜单顺序为打开主界面、快捷管理区域、刷新、设置、关于，退出位于底部并以分隔线隔开。每页均保留本程序开关，可点击“TrayPilot（本程序）”显示或隐藏自身托盘图标，也可在设置中切换。隐藏后再次运行程序会唤回已有主窗口。

菜单包含 **打开主界面 / 退出 / 关于**，以及当前识别到的软件图标列表。带勾（√）表示该软件的所有托盘图标正常显示；未勾选表示至少一个被隐藏。左键单击菜单中的软件即可切换：全部显示时隐藏，存在隐藏项时全部显示。同一路径的多个实例一起控制，主界面的搜索过滤不会限制这份菜单。自动刷新关闭时，可通过菜单里的 **刷新** 更新列表。

主界面和托盘菜单中的 **关于** 显示 TrayPilot 名称、版本、用途、系统信息、程序路径、设置目录及语言包目录。

在 **设置** 中分别配置三个快捷键：**打开主界面**（预置 Ctrl+Alt+M）、**显示所有**（Ctrl+Alt+S）、**隐藏规则命中**（Ctrl+Alt+H）。默认均不注册，勾选启用后，在输入框按 Ctrl 或 Alt 加其他键并保存。原“快捷管理”快捷键已移除，旧绑定不会自动转用于新操作。冲突或重复组合会提示更换，保留原有设置。

**显示所有** 显示已识别的隐藏图标及本程序托盘图标，保留隐藏规则，并暂停自动隐藏，避免下一次刷新立即隐藏。主界面按钮和快捷键 **隐藏规则命中** 按保存的规则隐藏匹配图标，清除临时显示例外，并恢复自动隐藏。暂停状态会保存，重启后仍然有效。

**设置 → 外观** 提供“跟随系统 / 浅色 / 深色”，默认跟随系统，并响应系统主题变化。主题统一应用到主界面、设置、关于、隐藏规则和菜单。本程序使用新绘制的蓝色托盘图标，程序文件、窗口和系统托盘共用同一设计。

## 多语言

在 **设置 → 语言** 选择 **简体中文 / English** 并保存，界面立即切换。默认简体中文，语言、关闭到托盘选项、本程序图标显示状态、主题、自动隐藏暂停状态和三个快捷键都会保存并在下次启动时使用。旧版设置会自动采用新增选项的默认值。

语言包独立位于 `app/languages/zh-CN.json` 和 `app/languages/en-US.json`，源文件位于 `source/languages`。UTF-8 JSON 格式：

```json
{
  "Name": "English",
  "Strings": {
    "关于": "About",
    "设置": "Settings"
  }
}
```

添加语言时复制现有完整文件，保留 `Strings` 中的键，只翻译值；保留 `{0}`、`{1}` 等格式占位符。文件名作为语言标识，`Name` 为设置里的名称。重新打开设置即可发现新语言文件。缺失翻译回退为简体中文；缺失或无效的语言包回退到默认语言，不影响图标管理。程序名称、文件路径和来自目标软件的提示文本保持原文。

## 自动识别范围

- 优先显示 Windows 缓存的托盘图标，缓存缺失或无效时使用 EXE 图标，再回退到通用应用图标。图标与提示缓存可能不反映软件的实时变化。
- 自动显示软件描述、进程名称、路径；鼠标悬停可查看详细信息。内容未变化时自动检查不会重建列表，便于持续查看悬浮信息。
- 软件名称主要来自 EXE 的文件描述；某些脚本可能显示 AutoHotkey 等宿主名称，可结合路径和提示文字辨认。
- 不要求手动输入进程名。只列出成功匹配到当前图标的记录，不把所有历史安装软件列为可控制项。
- 支持 NVIDIA 设置以及“安全删除硬件并弹出媒体”；对无法直接读取进程信息的图标，使用系统查询补充完整路径与创建时间，并继续核对归属。
- “安全删除硬件并弹出媒体”支持显示/隐藏及命中规则，不提供结束资源管理器进程的操作。其他音量、网络、电池、时钟等 Explorer 内建系统控件仍不管理。
- 依赖 Windows 的 `NotifyIconSettings` 记录。无记录、路径匹配失败、受保护进程或特殊实现可能无法识别或控制。
- Windows 的缓存和状态返回行为不是稳定的第三方托盘管理协议；当前版本属于针对 25H2 验证的原型，不能保证所有未来补丁和软件兼容。

## 配置与恢复

设置与恢复记录保存在 `%LOCALAPPDATA%\TrayPilot\settings.json`。程序不修改其他软件配置，也不写入 Windows 托盘注册表设置。

隐藏前先保存恢复记录，正常退出时恢复。如果管理器被强制结束，可重新打开 TrayPilot，再点击“全部恢复”。也可以退出并重新启动目标软件，让它重新创建图标。不要在仍有图标被隐藏时删除恢复记录。

## 技术实现

读取 `HKCU\Control Panel\NotifyIconSettings`，解析 Known Folder 路径；枚举窗口和消息窗口并核对进程路径，用 `Shell_NotifyIconGetRect` 校验图标存在性。

使用 `Shell_NotifyIcon(NIM_MODIFY)` 配合 `NIF_STATE`、`NIS_HIDDEN` 更新隐藏状态。GUID 图标使用 `NIF_GUID`。显示/隐藏操作不删除图标，也不结束目标进程；只有显式选择“结束任务”才终止目标进程。不注入 DLL，不修改 Explorer 内存。

在测试系统上，`Shell_NotifyIconGetRect` 返回 S_OK 表示正常显示（包含折叠区），S_FALSE 表示 NIS_HIDDEN。后者属于实测行为，微软文档并未承诺这一状态分类；系统更新后应重新验证。

## 验证与源码

测试结果见 `test-report.txt`。验证包括默认开启及可关闭的自动刷新、列表与网格切换、两种布局中的单击 / 右键不直接切换状态、双击隐藏和恢复、右键管理菜单及动态文字、属性信息、结束任务与过期进程身份拒绝、刷新期间搜索框内容和选区保持、状态文字、搜索、多选保留、图标透明度与悬浮信息；新增检查覆盖独立语言包、语言切换、只读属性表格、托盘快捷管理及勾选状态、关闭到托盘与真正退出、全局快捷键注册/冲突/释放、设置持久化；同时验证 UID/GUID 图标识别、跨进程隐藏恢复、规则持久化、退出恢复及目标软件重启。

所有源码位于 `source`。安装 .NET 8 或更新 SDK 后：

```powershell
dotnet publish .\source\TrayPilot.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o .\app
```

诊断命令（测试只操作独立测试程序的图标）：

```powershell
.\app\TrayPilot.exe --scan .\scan.json
.\app\TrayPilot.exe --self-test .\test-report.txt
```

参考：
- https://learn.microsoft.com/en-us/windows/win32/api/shellapi/ns-shellapi-notifyicondataw
- https://learn.microsoft.com/en-us/windows/win32/api/shellapi/nf-shellapi-shell_notifyicongetrect
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-findwindowexw
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-registerhotkey

本工具是独立编写的原型，不隶属于 Microsoft 或 Windhawk。
